Antes de codar, releia processing.consumer.spec.ts (já existente) e docs/FEATURES.md seção 4, item 2.

Quero duas melhorias nos testes já existentes de ProcessingConsumer, sem remover nem alterar os testes atuais de roteamento de confiança (READY/NEEDS_REVIEW):

Fortalecer as asserções existentes sobre extractionResult.create. Hoje o mock de prisma.extractionResult.create só confirma que foi chamado (retorno genérico {}), sem verificar os dados enviados. Em cada um dos dois testes já existentes (confiança alta e confiança baixa), adicione uma asserção expect(prisma.extractionResult.create).toHaveBeenCalledWith(...) confirmando que o documentId, extractedFields, suggestedFilename e confidenceScore retornados pelo AiPort mockado foram de fato passados para a criação do ExtractionResult — não só que o status do documento mudou. Isso garante que o resultado da IA realmente chega ao banco, não só o status.
Adicione um novo teste cobrindo o cenário de falha da IA dentro de uma tentativa (sem esgotar o retry). Cenário: aiPort.classifyAndExtract mockado para rejeitar (lançar erro, ex. mockRejectedValue(new Error('IA indisponível'))). O Document.status deve ser atualizado para PROCESSING normalmente no início, e o método process() do consumer deve propagar/relançar o erro (para o BullMQ saber que a tentativa falhou e acionar o retry — conforme a decisão registrada em docs/ARCHITECTURE.md de deixar o BullMQ controlar o retry, não fazer try/catch manual escondendo a falha). Confirme que prisma.extractionResult.create não foi chamado nesse cenário, já que a extração falhou antes de chegar lá.

Antes de gerar o código, me explique rapidamente por que o consumer deve relançar o erro em vez de capturá-lo silenciosamente — isso conecta com o próximo cenário de teste que vamos fazer depois (retry até FAILED).